# gutendocx
A modular DOCX processing engine and WebApp for publishers. Normalizes styles, rebuilds layout, inserts TOC, and generates aligned PDF via LibreOffice (Docker + PyUNO). Includes FastAPI backend, web UI, custom layout/sections pipeline and configurable YAML-driven styling.
